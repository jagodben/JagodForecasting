# API URL - set this to your Render backend URL in production
VITE_API_URL=http://localhost:5000/api

# API Key - must match the API key configured on the backend
VITE_API_KEY=your-secret-api-key-here
